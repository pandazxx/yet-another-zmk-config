# Pointing device (mouse) support, required by the joystick input listener.
CONFIG_ZMK_POINTING=y

# The analog stick driver selects ADC itself, but the nRF SAADC backend is
# picked up from devicetree and wants the subsystem on explicitly.
CONFIG_ADC=y
